<?xml version="1.0" encoding="utf-8"?>
<Grid xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
      xmlns:vm="clr-namespace:CrossCam.ViewModel"
      xmlns:vc="clr-namespace:CrossCam.ValueConverter"
      xmlns:model="clr-namespace:CrossCam.Model"
      xmlns:localization="clr-namespace:CrossCam.Resources.Localization"
      x:Class="CrossCam.CustomElement.LazyViews.PreviewMethodSettingsView"
      x:DataType="vm:SettingsViewModel"
      ColumnSpacing="0"
      RowSpacing="0">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="10"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="50"/>
        <ColumnDefinition Width="65"/>
        <ColumnDefinition Width="10"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
        <RowDefinition Height="50"/>
        <RowDefinition Height="60"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Border Grid.Row="0"
       Grid.Column="1"
       Grid.ColumnSpan="2"
       HorizontalOptions="FillAndExpand">
        <Picker ItemsSource="{Binding Modes}"
                ItemDisplayBinding="{Binding ., Converter={vc:EnumGetLocalizationConverter}}"
                SelectedIndex="{Binding Settings.Mode, Converter={vc:ModeValueConverter}}"/>
    </Border>
    <Image Grid.Row="0"
       Grid.Column="3" 
       Source="help.png"
       IsVisible="{Binding Settings.Mode, Converter={vc:EnumCompareConverter}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}">
        <Image.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding NavigateToFaqPageAndSection}" 
                              CommandParameter="{x:Static model:FaqScrollOptions.Cardboard}"/>
        </Image.GestureRecognizers>
    </Image>

    <Label Grid.Row="1"
       Grid.Column="1"
       Text="{x:Static localization:AppResources.Settings_Preview_Mirror}"/>
    <Image Grid.Row="1"
       Grid.Column="2" 
       Source="help.png">
        <Image.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding NavigateToFaqPageAndSection}" 
                              CommandParameter="{x:Static model:FaqScrollOptions.Mirror}"/>
        </Image.GestureRecognizers>
    </Image>
    <Switch Grid.Row="1"
        Grid.Column="3" 
        IsToggled="{Binding Settings.IsCaptureInMirrorMode}"/>

    <Label Grid.Row="2"
       Grid.Column="1"
       Grid.ColumnSpan="2"
       Text="{x:Static localization:AppResources.Settings_Preview_Immersive}"
       IsVisible="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"/>
    <Switch Grid.Row="2"
        Grid.Column="3"
        IsToggled="{Binding Settings.CardboardSettings.ImmersiveCardboardFinal}"
        IsVisible="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"/>

    <Label Grid.Row="3"
       Grid.Column="1" 
       Grid.ColumnSpan="2"
       Text="{x:Static localization:AppResources.Settings_Preview_CardboardDistance}"
       IsVisible="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"/>
    <Border Grid.Row="3" 
       Grid.Column="3" 
       IsVisible="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}">
        <Entry Text="{Binding Settings.CardboardSettings.CardboardIpd}"/>
    </Border>

    <Label Grid.Row="4"
       Grid.Column="1"
       Grid.ColumnSpan="2"
       Text="{x:Static localization:AppResources.Settings_Preview_Barrel}"
       IsVisible="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"/>
    <Switch Grid.Row="4" 
        Grid.Column="3" 
        IsToggled="{Binding Settings.CardboardSettings.AddBarrelDistortion}"
        IsVisible="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"/>

    <Label Grid.Row="5"
       Grid.Column="1" 
       Grid.ColumnSpan="2"
       Text="{x:Static localization:AppResources.Settings_Preview_BarrelFinal}"
       IsVisible="False">
        <Label.Triggers>
            <MultiTrigger TargetType="Label">
                <MultiTrigger.Conditions>
                    <BindingCondition Binding="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"
                                  Value="True"/>
                    <BindingCondition Binding="{Binding Settings.CardboardSettings.AddBarrelDistortion}"
                                  Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="IsVisible"
                    Value="True"/>
            </MultiTrigger>
        </Label.Triggers>
    </Label>
    <Switch Grid.Row="5"
        Grid.Column="3" 
        IsToggled="{Binding Settings.CardboardSettings.AddBarrelDistortionFinalOnly}"
        IsVisible="False">
        <Switch.Triggers>
            <MultiTrigger TargetType="Switch">
                <MultiTrigger.Conditions>
                    <BindingCondition Binding="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"
                                  Value="True"/>
                    <BindingCondition Binding="{Binding Settings.CardboardSettings.AddBarrelDistortion}"
                                  Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="IsVisible"
                    Value="True"/>
            </MultiTrigger>
        </Switch.Triggers>
    </Switch>

    <Label Grid.Row="6"
       Grid.Column="1" 
       Grid.ColumnSpan="2"
       Text="{x:Static localization:AppResources.Settings_Preview_LensStrength}"
       IsVisible="False">
        <Label.Triggers>
            <MultiTrigger TargetType="Label">
                <MultiTrigger.Conditions>
                    <BindingCondition Binding="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"
                                  Value="True"/>
                    <BindingCondition Binding="{Binding Settings.CardboardSettings.AddBarrelDistortion}"
                                  Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="IsVisible"
                    Value="True"/>
            </MultiTrigger>
        </Label.Triggers>
    </Label>
    <Border Grid.Row="6"
       Grid.Column="3" 
       IsVisible="False">
        <Border.Triggers>
            <MultiTrigger TargetType="Border">
                <MultiTrigger.Conditions>
                    <BindingCondition Binding="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"
                                  Value="True"/>
                    <BindingCondition Binding="{Binding Settings.CardboardSettings.AddBarrelDistortion}"
                                  Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="IsVisible"
                    Value="True"/>
            </MultiTrigger>
        </Border.Triggers>
        <Entry Text="{Binding Settings.CardboardSettings.CardboardBarrelDistortion}"/>
    </Border>

    <Label Grid.Row="7"
       Grid.Column="1"
       Grid.ColumnSpan="2"
       Text="{x:Static localization:AppResources.Settings_Preview_Downsize}"
       IsVisible="False">
        <Label.Triggers>
            <MultiTrigger TargetType="Label">
                <MultiTrigger.Conditions>
                    <BindingCondition Binding="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"
                                  Value="True"/>
                    <BindingCondition Binding="{Binding Settings.CardboardSettings.AddBarrelDistortion}"
                                  Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="IsVisible"
                    Value="True"/>
            </MultiTrigger>
        </Label.Triggers>
    </Label>
    <Switch Grid.Row="7"
        Grid.Column="3"
        IsToggled="{Binding Settings.CardboardSettings.CardboardDownsize}"
        IsVisible="False">
        <Switch.Triggers>
            <MultiTrigger TargetType="Switch">
                <MultiTrigger.Conditions>
                    <BindingCondition Binding="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"
                                  Value="True"/>
                    <BindingCondition Binding="{Binding Settings.CardboardSettings.AddBarrelDistortion}"
                                  Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="IsVisible"
                    Value="True"/>
            </MultiTrigger>
        </Switch.Triggers>
    </Switch>

    <Label Grid.Row="8"
       Grid.Column="1"
       Grid.ColumnSpan="2"
       Text="{x:Static localization:AppResources.Settings_Preview_DownsizePercent}"
       IsVisible="False">
        <Label.Triggers>
            <MultiTrigger TargetType="Label">
                <MultiTrigger.Conditions>
                    <BindingCondition Binding="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"
                                  Value="True"/>
                    <BindingCondition Binding="{Binding Settings.CardboardSettings.CardboardDownsize}"
                                  Value="True"/>
                    <BindingCondition Binding="{Binding Settings.CardboardSettings.AddBarrelDistortion}"
                                  Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="IsVisible"
                    Value="True"/>
            </MultiTrigger>
        </Label.Triggers>
    </Label>
    <Border Grid.Row="8"
       Grid.Column="3"
       IsVisible="False">
        <Border.Triggers>
            <MultiTrigger TargetType="Border">
                <MultiTrigger.Conditions>
                    <BindingCondition Binding="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"
                                  Value="True"/>
                    <BindingCondition Binding="{Binding Settings.CardboardSettings.CardboardDownsize}"
                                  Value="True"/>
                    <BindingCondition Binding="{Binding Settings.CardboardSettings.AddBarrelDistortion}"
                                  Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="IsVisible"
                    Value="True"/>
            </MultiTrigger>
        </Border.Triggers>
        <Entry Text="{Binding Settings.CardboardSettings.CardboardDownsizePercentage}"/>
    </Border>

    <Button Grid.Row="9"
        Grid.Column="1"
        Grid.ColumnSpan="3" 
        Text="{x:Static localization:AppResources.Settings_Reset}"
        Command="{Binding ResetCardboardToDefaults}"
        IsVisible="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Cardboard}}"/>

    <Label Grid.Row="2"
       Grid.Column="1" 
       Grid.ColumnSpan="2"
       Text="{x:Static localization:AppResources.Settings_Preview_Width}"
       IsVisible="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Parallel}}"/>
    <Border Grid.Row="2"
       Grid.Column="3" 
       IsVisible="{Binding Settings.Mode, Converter={StaticResource enumCompare}, ConverterParameter={x:Static vm:DrawMode.Parallel}}">
        <Entry Text="{Binding Settings.MaximumParallelWidth}"/>
    </Border>
</Grid>